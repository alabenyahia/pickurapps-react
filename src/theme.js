import {createMuiTheme} from "@material-ui/core";

export const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#673ab7',
            light: '#9a67ea',
            dark: '#320b86'
        },
        secondary: {
            main: '#ff4081',
            light: '#ff79b0',
            dark: '#c60055'
        }
    }
});